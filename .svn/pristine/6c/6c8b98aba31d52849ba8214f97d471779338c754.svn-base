// PC端在线编辑 文件类型限制
const pcTypeLimit = ['html']
/**
 * 判断pc端此类型是否可以在线编辑
 * @param {String} val 文件类型后缀
 * @return {Boolean}
 */
function pcTypeCheck (val) {
  return !pcTypeLimit.includes(val.toLowerCase())
}
export default {
  pcTypeCheck
}